import 'package:flutter/widgets.dart';

class AppScaffoldPlaceholder extends StatelessWidget {
  const AppScaffoldPlaceholder({super.key, required this.child});

  final Widget child;

  @override
  Widget build(BuildContext context) {
    return child;
  }
}
